// next.config.ts

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true, // Optional, but can help with development errors
};

module.exports = nextConfig; // Export config